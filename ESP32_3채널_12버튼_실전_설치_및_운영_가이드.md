# ESP32 3채널 12버튼 실전 설치 및 운영 가이드

이 문서는 ESP32-S3 두 대와 물리 버튼 12개를 실제로 연결하고, 세 개의 게임 화면을 동시에 운영하기 위해 사용자가 해야 할 일을 순서대로 정리한 안내서입니다.

## 1. 최종 구성 이해하기

두 ESP32의 역할은 완전히 다릅니다. 작업 전에 스티커나 테이프로 `ESP32 A`와 `ESP32 B`를 표시해 두는 것이 좋습니다.

```text
버튼 12개
    │
    ▼
ESP32 B · Controller
    │  Wi-Fi + UDP 4210
    ▼
ESP32 A · Hub
    ├─ hihi Wi-Fi 생성
    ├─ 웹페이지 제공
    └─ WebSocket으로 버튼 상태 전달
         │
         ├─ 화면 1: Channel A
         ├─ 화면 2: Channel B
         └─ 화면 3: Channel C
```

- **ESP32 A · Hub:** 버튼을 직접 연결하지 않습니다. Wi-Fi와 게임 웹페이지를 제공합니다.
- **ESP32 B · Controller:** 물리 버튼 12개를 연결합니다. 버튼 입력만 읽어 A로 전송합니다.
- **화면:** 노트북, PC, 태블릿 등 브라우저를 사용할 수 있는 기기입니다. 모두 A가 만든 `hihi` Wi-Fi에 연결해야 합니다.

## 2. 준비물

### 필수 준비물

- ESP32-S3 DevKitM-1 보드 2대
- 누르는 동안만 연결되는 일반 푸시 버튼 12개
- 브레드보드 또는 단자대
- 점퍼선
- USB 데이터 케이블 2개
- 펌웨어를 업로드할 PC 또는 Mac 1대
- 게임을 띄울 브라우저 화면 3대

### 있으면 좋은 준비물

- ESP32 A/B 구분용 라벨
- 버튼 1~12 번호 라벨
- 멀티탭과 안정적인 USB 전원 어댑터
- GND를 여러 버튼에 나누기 위한 공통 GND 레일
- 행사 현장용 예비 USB 케이블과 예비 버튼

> USB 충전 전용 케이블은 펌웨어 업로드가 되지 않습니다. 반드시 데이터 통신이 되는 케이블을 사용하세요.

## 3. 소프트웨어 준비

이 프로젝트는 PlatformIO 기준입니다.

1. VS Code를 설치합니다.
2. VS Code 확장 프로그램에서 `PlatformIO IDE`를 설치합니다.
3. VS Code에서 이 프로젝트 폴더를 엽니다.
4. VS Code 하단의 PlatformIO 초기화가 끝날 때까지 기다립니다.
5. 터미널에서 다음 명령으로 세 펌웨어가 빌드되는지 확인합니다.

```bash
pio run -e esp32-s3-devkitm-1 -e esp32-s3-hub -e esp32-s3-controller
```

정상이면 마지막에 세 환경 모두 `SUCCESS`가 표시됩니다.

## 4. ESP32 B에 버튼 12개 연결하기

### 기본 배선 원칙

ESP32 내부 Pull-up 저항을 사용하므로 외부 저항은 필요하지 않습니다.

```text
ESP32 GPIO ───── 버튼 ───── ESP32 GND
```

- 버튼을 누르지 않음: `HIGH`
- 버튼을 누름: `LOW`
- 모든 버튼의 GND 쪽은 같은 GND 레일에 연결해도 됩니다.
- ESP32 A에는 버튼을 연결하지 않습니다.

### 버튼별 연결표

| 물리 버튼 라벨 | ESP32 B GPIO | 채널 | 채널 내부 버튼 | 게임에서의 기본 의미 |
| :---: | :---: | :---: | :---: | :--- |
| 1 | GPIO 4 | A | 1 | 전진 / 위 / P1 |
| 2 | GPIO 5 | A | 2 | 후진 / 아래 / P2 |
| 3 | GPIO 6 | A | 3 | 왼쪽 / P3 |
| 4 | GPIO 7 | A | 4 | 오른쪽 / P4 |
| 5 | GPIO 8 | B | 1 | 전진 / 위 / P1 |
| 6 | GPIO 9 | B | 2 | 후진 / 아래 / P2 |
| 7 | GPIO 10 | B | 3 | 왼쪽 / P3 |
| 8 | GPIO 11 | B | 4 | 오른쪽 / P4 |
| 9 | GPIO 12 | C | 1 | 전진 / 위 / P1 |
| 10 | GPIO 13 | C | 2 | 후진 / 아래 / P2 |
| 11 | GPIO 14 | C | 3 | 왼쪽 / P3 |
| 12 | GPIO 15 | C | 4 | 오른쪽 / P4 |

### 배선할 때 지킬 순서

1. ESP32 B의 USB 전원을 분리합니다.
2. 브레드보드의 한쪽 레일을 ESP32 B의 `GND`와 연결합니다.
3. 버튼 12개의 한쪽 단자를 공통 GND 레일에 연결합니다.
4. 각 버튼의 반대쪽 단자를 위 표의 GPIO에 연결합니다.
5. 버튼 라벨과 실제 GPIO가 일치하는지 한 번 더 확인합니다.
6. 합선된 선이 없는지 확인한 뒤 USB를 연결합니다.

> 보드에 적힌 핀 이름이 표와 다르거나 GPIO 4~15 중 일부가 노출되지 않은 보드라면 바로 업로드하지 말고 보드 핀맵을 먼저 확인하세요. 핀은 `src/controller/main.cpp`의 `BUTTON_PINS` 배열에서 변경할 수 있습니다.

## 5. ESP32 A · Hub 업로드하기

처음에는 실수를 줄이기 위해 ESP32 A만 PC에 연결하고 ESP32 B는 분리해 둡니다.

### 5-1. 펌웨어 업로드

프로젝트 최상위 폴더의 터미널에서 실행합니다.

```bash
pio run -e esp32-s3-hub -t upload
```

### 5-2. 게임 웹 파일 업로드

Hub에는 펌웨어뿐 아니라 HTML, CSS, JavaScript와 이미지도 업로드해야 합니다.

```bash
pio run -e esp32-s3-hub -t uploadfs
```

웹 화면을 수정한 경우에는 `uploadfs`를 다시 실행해야 실제 ESP32 화면에도 반영됩니다.

### 5-3. 포트를 자동으로 찾지 못할 때

연결된 장치를 확인합니다.

```bash
pio device list
```

표시된 포트가 `/dev/cu.usbmodemXXXX`라고 가정하면 다음처럼 지정합니다.

```bash
pio run -e esp32-s3-hub -t upload --upload-port /dev/cu.usbmodemXXXX
pio run -e esp32-s3-hub -t uploadfs --upload-port /dev/cu.usbmodemXXXX
```

Windows에서는 `/dev/cu...` 대신 `COM3`, `COM4` 같은 포트 이름을 사용합니다.

## 6. ESP32 B · Controller 업로드하기

ESP32 A를 PC에서 분리하거나 포트를 정확히 구분한 뒤 ESP32 B를 연결합니다.

```bash
pio run -e esp32-s3-controller -t upload
```

Controller에는 웹 파일이 없으므로 `uploadfs`를 실행하지 않습니다.

포트를 직접 지정해야 한다면 다음처럼 실행합니다.

```bash
pio run -e esp32-s3-controller -t upload --upload-port /dev/cu.usbmodemXXXX
```

## 7. 최초 전원 및 연결 확인

### 권장 전원 순서

1. ESP32 A · Hub의 전원을 먼저 켭니다.
2. 3~5초 기다립니다.
3. 휴대폰이나 노트북의 Wi-Fi 목록에서 `hihi`가 나타나는지 확인합니다.
4. ESP32 B · Controller의 전원을 켭니다.
5. 다시 3~5초 기다립니다.

기본 네트워크 정보는 다음과 같습니다.

| 항목 | 값 |
| :--- | :--- |
| Wi-Fi 이름 | `hihi` |
| Wi-Fi 비밀번호 | `12345678` |
| Hub 주소 | `192.168.4.1` |
| 웹 포트 | `80` |
| WebSocket 포트 | `81` |
| Controller UDP 포트 | `4210` |

`hihi`는 인터넷이 없는 로컬 게임 전용 Wi-Fi입니다. 기기에서 “인터넷 없음” 경고가 표시되어도 연결을 유지해야 합니다.

## 8. 시리얼 모니터로 두 보드 확인하기

보드 하나를 PC에 연결한 상태에서 아래 명령을 사용합니다.

### ESP32 A 로그

```bash
pio device monitor -e esp32-s3-hub
```

정상 시작 시 비슷한 내용이 보입니다.

```text
[Hub] ESP32-S3 3-channel game hub starting
[WiFi] AP hihi at 192.168.4.1
[UDP] Listening on 4210
[WS] Listening on ws://192.168.4.1:81
```

ESP32 B가 연결되고 버튼을 누르면 다음과 같은 로그가 보여야 합니다.

```text
[Controller] ONLINE
[UDP] A-1 DOWN seq=...
[UDP] A-1 UP seq=...
```

### ESP32 B 로그

```bash
pio device monitor -e esp32-s3-controller
```

정상 연결 및 입력 시 비슷한 내용이 보입니다.

```text
[WiFi] Connected, IP: 192.168.4.x
[BUTTON] A-1 DOWN (GPIO 4)
[UDP] A-1 DOWN seq=...
```

시리얼 모니터 종료 단축키는 일반적으로 `Ctrl+C`입니다.

## 9. 12버튼 테스트부터 진행하기

게임을 먼저 열지 말고 전체 버튼 테스트부터 완료합니다.

1. 테스트용 노트북이나 태블릿을 `hihi` Wi-Fi에 연결합니다.
2. 브라우저에서 `http://192.168.4.1`을 엽니다.
   - 게임 화면에서는 페이지 맨 아래의 흐린 `관리자 · 버튼 상태 점검` 링크로 들어갈 수도 있습니다.
   - 버튼 점검은 일반 게임 탭에는 표시되지 않으며, 게임 플레이가 시작되면 하단 링크도 숨겨집니다.
3. 화면 상단에서 다음 상태를 확인합니다.
   - `Hub ONLINE`
   - `Controller ONLINE`
4. 버튼 1부터 12까지 하나씩 누릅니다.
5. 해당 Channel A/B/C 카드가 `UP → DOWN → UP`으로 바뀌는지 확인합니다.
6. 누른 횟수가 한 번 누를 때 정확히 1씩 증가하는지 확인합니다.
7. 같은 채널에서 두 버튼을 동시에 눌러 두 카드가 함께 `DOWN`이 되는지 확인합니다.
8. 서로 다른 채널의 버튼도 동시에 눌러봅니다.

### 이 단계의 완료 기준

- 버튼 1~4는 Channel A에서만 반응합니다.
- 버튼 5~8은 Channel B에서만 반응합니다.
- 버튼 9~12는 Channel C에서만 반응합니다.
- 한 번 눌렀을 때 횟수가 여러 번 올라가지 않습니다.
- 버튼을 놓으면 반드시 `UP`으로 돌아옵니다.
- Hub와 Controller가 모두 `ONLINE`입니다.

이 조건을 모두 만족하기 전에는 실제 게임 운영 단계로 넘어가지 않는 것이 좋습니다.

## 10. 세 개의 게임 화면 연결하기

화면으로 사용할 기기 세 대를 모두 `hihi` Wi-Fi에 연결합니다. 각 기기는 서로 다른 채널 URL을 엽니다.

### 기본 운영 예시

| 화면 | URL | 사용하는 물리 버튼 |
| :---: | :--- | :--- |
| 화면 1 | `http://192.168.4.1/index.html?channel=A` | 버튼 1~4 |
| 화면 2 | `http://192.168.4.1/traffic.html?channel=B` | 버튼 5~8 |
| 화면 3 | `http://192.168.4.1/jump-rope.html?channel=C` | 버튼 9~12 |

- `?channel=A`, `?channel=B`, `?channel=C`가 각 화면이 받을 버튼 묶음을 결정합니다.
- 채널을 생략하거나 잘못 입력하면 Channel A로 동작합니다.
- 게임 메뉴에서 다른 게임으로 이동해도 현재 선택한 채널이 유지됩니다.
- 키보드와 화면 터치 버튼은 해당 브라우저에서 로컬 테스트용으로 계속 사용할 수 있습니다.

## 11. 실제 행사 전 전체 리허설

아래 순서로 최소 한 번은 전체 리허설을 진행합니다.

### 기능 점검

- [ ] ESP32 A를 켜면 `hihi` Wi-Fi가 생성된다.
- [ ] ESP32 B가 자동으로 A에 연결된다.
- [ ] 테스트 페이지에서 Hub/Controller가 ONLINE이다.
- [ ] 12개 버튼이 각각 올바른 채널에서 반응한다.
- [ ] 세 화면이 각각 지정된 채널 입력만 받는다.
- [ ] 세 채널에서 동시에 버튼을 눌러도 입력이 섞이지 않는다.
- [ ] 각 게임의 준비 화면과 다시하기 입력이 정상 동작한다.

### 연결 복구 점검

- [ ] 버튼을 누른 상태에서 ESP32 B 전원을 끈다.
- [ ] 약 3초 안에 Controller가 OFFLINE이 되고 모든 버튼 UI가 UP으로 해제된다.
- [ ] ESP32 B를 다시 켜면 Controller가 자동으로 ONLINE으로 돌아온다.
- [ ] 화면 기기의 Wi-Fi를 껐다 켜도 웹페이지가 다시 연결된다.
- [ ] Wi-Fi를 껐다 켜도 OS의 "인터넷 없음" 알림이나 Wi-Fi 로그인 창이 다시 뜨지 않는다.
- [ ] 브라우저를 새로고침하면 현재 버튼 상태가 정상적으로 복구된다.

### 현장 준비 점검

- [ ] 모든 USB 전원과 케이블이 쉽게 빠지지 않도록 고정했다.
- [ ] 버튼 1~12와 Channel A/B/C 라벨이 잘 보인다.
- [ ] 세 화면의 절전 모드와 자동 화면 꺼짐을 해제했다.
- [ ] 각 브라우저를 전체화면으로 열었다.
- [ ] 행사 시작 직전에 12버튼 테스트를 다시 실행했다.

## 12. 문제가 생겼을 때 확인 순서

항상 아래 순서대로 확인하면 문제 지점을 빠르게 찾을 수 있습니다.

```text
1. ESP32 B 시리얼 버튼 로그
        ↓
2. ESP32 B UDP 전송 로그
        ↓
3. ESP32 A UDP 수신 로그
        ↓
4. 12버튼 웹 테스트 화면
        ↓
5. 실제 게임과 채널 URL
```

### 버튼을 눌러도 ESP32 B 로그가 나오지 않는 경우

- 버튼이 해당 GPIO와 GND 사이에 연결됐는지 확인합니다.
- 버튼 다리 방향이 잘못되지 않았는지 확인합니다.
- GPIO 번호와 버튼 라벨을 다시 비교합니다.
- 점퍼선과 브레드보드 접촉 상태를 확인합니다.

### ESP32 B에는 버튼 로그가 있지만 UDP 로그가 없는 경우

- ESP32 A의 전원이 켜져 있는지 확인합니다.
- `hihi` Wi-Fi가 실제로 생성됐는지 확인합니다.
- A와 B의 SSID와 비밀번호 설정이 같은지 확인합니다.
- B 로그에 `[WiFi] Connected`가 표시되는지 확인합니다.

### A에서 UDP 로그는 보이지만 웹 화면이 반응하지 않는 경우

- 화면 기기가 `hihi` Wi-Fi에 연결되어 있는지 확인합니다.
- 주소가 `https://`가 아니라 `http://192.168.4.1`인지 확인합니다.
- 브라우저를 새로고침합니다.
- Hub 펌웨어 업로드 후 `uploadfs`도 실행했는지 확인합니다.

### 테스트 페이지는 정상인데 특정 게임만 반응하지 않는 경우

- 게임 URL 끝의 `?channel=A|B|C`를 확인합니다.
- 화면과 실제 버튼 묶음이 같은 채널인지 확인합니다.
- 예를 들어 버튼 5~8을 쓸 화면은 반드시 `?channel=B`여야 합니다.

### 버튼이 계속 눌린 상태로 남는 경우

- 정상 구조에서는 500ms 상태 재전송과 3초 타임아웃이 자동으로 해제합니다.
- 3초 이상 지나도 남는다면 브라우저를 새로고침합니다.
- ESP32 A와 B를 차례로 재부팅하고 테스트 페이지에서 다시 확인합니다.

### 게임 중 "인터넷 없음" 알림이나 Wi-Fi 로그인 창이 다시 뜨는 경우

OS는 Wi-Fi에 연결되거나 연결이 복구될 때마다 자체 인터넷 연결 확인 주소
(Windows NCSI, Apple hotspot-detect, Android/Chrome generate_204, Firefox success.txt)를
다시 호출합니다. 펌웨어는 이 주소들을 OS가 기대하는 응답으로 그대로 돌려주고,
알 수 없는 비페이지 요청은 게임 페이지로 리다이렉트하지 않고 204로 응답합니다.

- Hub 펌웨어가 최신인지 확인합니다. (아래 "코드를 수정한 뒤 다시 올리는 방법" 참고)
- 세 화면 기기 모두 `hihi` Wi-Fi에 연결되어 있고 인터넷 없음 경고를 무시하고 연결을 유지합니다.
- 그럼에도 로그인 창이 반복해서 뜨면 Hub 펌웨어를 다시 업로드한 뒤 기기의 Wi-Fi를 껐다 켭니다.

## 13. 코드를 수정한 뒤 다시 올리는 방법

### 웹 화면만 수정한 경우

ESP32 A의 LittleFS만 다시 업로드합니다.

```bash
pio run -e esp32-s3-hub -t uploadfs
```

### Hub 통신 코드를 수정한 경우

```bash
pio run -e esp32-s3-hub -t upload
```

웹 파일도 함께 수정했다면 `uploadfs`도 실행합니다.

### Controller 버튼·GPIO 코드를 수정한 경우

```bash
pio run -e esp32-s3-controller -t upload
```

### 전체 자동 테스트

```bash
cd server
npm test
```

테스트가 끝난 뒤 프로젝트 최상위 폴더로 돌아가려면 다음을 실행합니다.

```bash
cd ..
```

## 14. 기존 단일 ESP32 4버튼 모드 사용하기

기존 방식이 필요한 경우 다음 환경을 사용합니다.

```bash
pio run -e esp32-s3-devkitm-1 -t upload
pio run -e esp32-s3-devkitm-1 -t uploadfs
```

이 모드의 기존 4버튼 입력은 Channel A로 처리됩니다. 두 보드 12버튼 운영 시에는 `esp32-s3-hub`와 `esp32-s3-controller` 환경을 사용해야 합니다.

## 15. 가장 짧은 실행 순서

이미 배선과 업로드를 한 번 완료했다면 현장에서는 아래 여섯 단계만 따르면 됩니다.

1. ESP32 A 전원을 켭니다.
2. ESP32 B 전원을 켭니다.
3. 세 화면을 `hihi` Wi-Fi에 연결합니다.
4. `http://192.168.4.1`에서 12개 버튼을 검사합니다.
5. 세 화면에서 A/B/C 채널 게임 URL을 각각 엽니다.
6. 모든 게임에서 준비 입력을 확인한 뒤 운영을 시작합니다.
