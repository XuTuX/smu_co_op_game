# ESP32A 경량화 및 노트북 실행 가이드

> 가장 편한 방법: 다른 콤퓨터에는 `노트북_게임_실행용` 폴더 하나만 복사한 다음, 그 안의 `게임_실행.html`을 열면 됩니다.

## 1. 문서 목적

이 프로젝트는 대용량 HTML, CSS, JavaScript, 캐릭터 이미지, 음악 파일을 ESP32A에 저장하지 않고 노트북에서 직접 열어는 방식으로 바꿥니다.

ESP32A에느 다음 기능만 남겼습니다.

- `hihi` Wi-Fi 생성
- ESP32B가 보낸 UDP 버튼 수신
- 노트북에 WebSocket으로 버튼 상태 전송
- A/B/C 채널 분리

ESP32A에서느 다음 기능을 제거했습니다.

- HTTP 웹서버
- DNS 서비스
- LittleFS 파일 시스템
- ArduinoJson 의존성
- 게임·캐릭터·음악 파일

ESP32A 경량 펌워어는 빌드되었으며 펌워어 이미지는 약 720KB입니다. `client` 약 336KB와 `assets` 약 3.4MB는 노트북에만 남습니다.

## 2. 이해하기 쉬운 구조

```text
버튼 12개
    │
    ▼
ESP32B Controller
    │ UDP 4210
    ▼
ESP32A Lightweight Hub
    ├─ Wi-Fi: hihi
    └─ WebSocket: ws://192.168.4.1:81
                         │
                         ▼
               노트북의 로컬 HTML 게임
```

| 기기 | 역할 |
| --- | --- |
| ESP32A | Wi-Fi AP, UDP 수신, WebSocket 전송 |
| ESP32B | 버튼 12개 읽기 및 UDP 전송 |
| 노트북 | HTML/CSS/JS/이미지/음악 저장 및 게임 표시 |

## 3. 현재 업로드 상태

**현재는 ESP32A에 경량 펌워어를 업로드하지 않았습니다.**

이미 완료된 것:

- ESP32A 소스 코드 빌드
- ESP32A 경량 펌워어 컴파일
- 노트북 HTML 직접 열기
- A/B/C 채널 선택기
- 자동 테스트

이미 안 본 것:

- ESP32A에 일제 업로드
- `pio ... -t upload`
- `pio ... -t uploadfs`

## 4. 나중에 ESP32A에 펌워어 업로드하기

현재는 업로드하지 않았지만 나중에는 다음 순서를 따릅니다.

### 4-1. ESP32A만 연결

1. ESP32A와 ESP32B USB를 모두 뽑습니다.
2. ESP32A만 노트북에 USB로 연결합니다.
3. 어떤 보드가 ESP32A인지 반드시 확인합니다.

### 4-2. 포트 확인

터미널을 열고 프로젝트 폴더로 이동합니다.

```bash
cd "/Users/dd/Documents/side project/smu_팝업스토어/smu_co_op_game"
ls /dev/cu.usbmodem*
```

예시:

```text
/dev/cu.usbmodem5B8E0626251
```

포트 명칭은 USB를 다시 꼬지 또느 노트북을 재시작하면 바꿜 수 있습니다.

### 4-3. ESP32A 펌워어만 업로드

아래 명령에서 `XXXX`를 앞에서 확인한 포트로 바꿥니다.

```bash
pio run -e esp32-s3-hub -t upload --upload-port /dev/cu.usbmodemXXXX
```

예시:

```bash
pio run -e esp32-s3-hub -t upload --upload-port /dev/cu.usbmodem5B8E0626251
```

마지막 부분에 `SUCCESS`가 나오면 성공입니다.

### 4-4. `uploadfs`는 절대로 하지 마세요

다음 명령은 실행하지 마세요.

```bash
pio run -e esp32-s3-hub -t uploadfs
```

현재는 웹 파일을 ESP32A에 올리지 않으며, 위 명령은 실행할 필요도 없습니다.

## 5. 평소 실행 순서

### 5-1. 전원 켜기

1. ESP32A에 전원을 너ᄒ습니다.
2. ESP32B에도 전원을 너ᄒ습니다.
3. 두 보드가 부팅하도록 5초 정도 기다립니다.

### 5-2. 노트북 Wi-Fi 연결

1. macOS 우측 상단의 Wi-Fi 이이콘을 누릅니다.
2. `hihi`를 선택합니다.
3. 비밀번호 `12345678`을 입력합니다.
4. “인터넷 없음” 또느 “인터넷 연결 없음”이 나와도 그대로 유지합니다.

`hihi`는 인터넷이 아니라 ESP32A와 노트북 사이의 지역 통신이므로 그대로 유지합니다.

### 5-3. 게임 HTML 파일 열기

Finder에서 다음 폴더로 이동합니다.

```text
/Users/dd/Documents/side project/smu_팝업스토어/smu_co_op_game/client
```

| 파일 | 게임 |
| --- | --- |
| `index.html` | 버스 주차 |
| `traffic.html` | 장애물 피하기 |
| `jump-rope.html` | 단체 줄넘기 |
| `button-test.html` | A/B/C 12개 버튼 전체 테스트 |

원하는 HTML을 더블 클릭하면 기본 브라우저로 열립니다. Node.js, `npm start`, `localhost:3000`은 필요하지 않습니다.

## 6. 채널별로 원하는 게임 실행하기

게임과 채널은 고정되어 있지 않습니다. 어떤 채널이든 게임 HTML을 자유롭게 고를 수 있습니다.

### 창 하나에서 채널 고르는 순서

1. 원하는 게임 HTML을 열업니다.
2. 화면 우측 상단의 `채널` 선택기로 A, B, C 중 하나를 고릅니다.
3. 채널을 바꾸면 화면이 새로 고침됩니다.
4. 새로 고침 후 원하냐던 채널의 버튼만 반응합니다.

채널 선택은 현재 창 URL에만 저장되며 다른 창에는 영향을 주지 않습니다.

### 예시 1: A/B/C 모두 버스 주차

- 창 1: `index.html` + A
- 창 2: `index.html` + B
- 창 3: `index.html` + C

### 예시 2: 채널마다 다른 게임

- 창 1: `index.html` + A
- 창 2: `traffic.html` + B
- 창 3: `jump-rope.html` + C

### 예시 3: 원하는 조합

- A는 줄넘기
- B는 버스 주차
- C는 장애물 피하기

게임 메뉴로 다른 게임에 이동해도 현재 창의 채널이 유지됩니다.

## 7. 버튼 12개 전체 테스트

행사 시작 전 다음 파일을 열업니다.

```text
client/button-test.html
```

다음 항목을 점검합니다.

- `Hub ONLINE`: 노트북가 ESP32A WebSocket에 연결됨
- `Controller ONLINE`: ESP32A가 ESP32B의 UDP 입력을 받고 있음
- A-1 ~ A-4: A 채널 버튼
- B-1 ~ B-4: B 채널 버튼
- C-1 ~ C-4: C 채널 버튼

각 버튼을 하나씩 누르고 뗐을 때 해당 카드가 초록색으로 바꿔는지 확인합니다.

## 8. 웨 파일 수정 후 반영

노트북에 저장되는 파일:

- `client/*.html`
- `client/*.css`
- `client/js/*.js`
- `assets/charcter_movement/*`
- `assets/sound/*`

수정 후 순서:

1. 파일을 저장합니다.
2. 열려 있는 브라우저 화면을 새로 고침합니다.
3. 업로드나 `npm start`는 하지 않습니다.

이미지나 음악이 보이면 소리 재생을 위해 브라우저 보안 정책이 해제되고 음악을 재생한 후에 눌러야 합니다.

## 9. 문제 해결

### 9-1. HTML이 안 열릴 때

- `client` 폴더 안의 HTML을 열었는지 확인합니다.
- `client` 폴더와 `assets` 폴더가 가튼 프로젝트 안에 있는지 확인합니다.
- 북마크가 잡히면 Chrome 또는 Edge로 다시 열어보세요.

### 9-2. 게임은 나오지만 버튼 반응이 없을 때

1. 노트북이 `hihi`에 연결되어 있는지 확인합니다.
2. ESP32A/ESP32B 전원이 들어와 리셋이 켜져 있는지 확인합니다.
3. `button-test.html`에서 `Hub ONLINE` / `Controller ONLINE`을 확인합니다.
4. 선택한 채널와 버튼 채널이 가튼지 확인합니다.

### 9-3. `Hub ONLINE`, `Controller OFFLINE`일 때

ESP32A는 잘 동작하지만 ESP32B가 입력을 보내지 못하고 있다는 뜻입니다.

- ESP32B 전원을 껐다 켜거나 다시 너ᄒ습니다.
- ESP32B가 `hihi`에 자동 연결하는지 기다립니다.
- ESP32B의 펌워어가 아직 업로드되어 있는지 확인합니다.

### 9-4. `192.168.4.1`이 안 열릴 때

정상입니다. 경량화한 ESP32A는 HTTP 웹서버를 제거했기 때문이므로 브라우저에서 로컬 HTML을 열어야 합니다.

### 9-5. 북마크나 찾기가 안 나옴

- HTML이 `client` 폴더 안에 있는지 확인합니다.
- `assets` 폴더가 프로젝트 루트에 있는지 확인합니다.
- 음악은 화면을 한 번 눌러고 보안 정책을 먼저 합니다.

### 9-6. ESP32A 업로드 중 포트가 잡히는 경우

1. ESP32B 또느 다른 USB 보드를 뽑습니다.
2. 포트를 다시 확인합니다.
3. BOOT 버튼을 누른 채로 RST/EN 버튼을 한 번 누르고 뗍니다.
4. 가튼 포트로 다시 실행합니다.

## 10. 행사 운영 전 체크리스트

- [ ] ESP32A 전원 ON
- [ ] ESP32B 전원 ON
- [ ] 노트북 Wi-Fi는 `hihi`로 연결
- [ ] `button-test.html`에서 `Hub ONLINE`
- [ ] `button-test.html`에서 `Controller ONLINE`
- [ ] A/B/C 12개 버튼 반응 확인
- [ ] 게임 창별로 원하는 HTML 열기
- [ ] 창마다 채널 A/B/C 선택
- [ ] 음악 재생 확인
- [ ] 버튼 동시 입력 확인

## 11. 한 줄 요약

### 해야 할 것

1. ESP32A와 ESP32B의 전원을 너ᄒ습니다.
2. 노트북을 `hihi`에 연결합니다.
3. `client/button-test.html`에서 12개 버튼을 검사합니다.
4. 게임 HTML을 열고 창마다 채널을 고릅니다.
5. 게임을 시작합니다.

### 하지 말아야 할 것

- ESP32A에 `uploadfs`
- `npm start`
- `http://192.168.4.1`로 게임 열기
- ESP32A에 HTML/이미지/음악 업로드

웨 파일은 노트북에 두고, ESP32A는 Wi-Fi·UDP·WebSocket 중계만 합니다.
